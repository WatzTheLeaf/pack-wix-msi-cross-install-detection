# Prerequisites
- Install Wix6 `dotnet tool install --global wix`
- Run `wix extension add WixToolset.UI.wixext`

# Run
Run from Visual Studio Developer PowerShell.

- `./build-app.ps1`
- `./build-msi.ps1`